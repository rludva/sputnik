piVersion: v1
kind: Pod
metadata:
  name: sputnik
spec:
  containers:                            
  - image: quay.io/rludva/sputnik:latest
    name: sputnik
    ports:                              
    - containerPort: 8080
      protocol: TCP
  restartPolicy: Always
